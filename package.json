{
  "name": "occam-dom",
  "author": "James Smith",
  "version": "3.0.8",
  "license": "MIT, Anti-996",
  "homepage": "https://github.com/jecs-imperial/occam-dom",
  "description": "Occam's DOM related functionality.",
  "repository": {
    "type": "git",
    "url": "https://github.com/jecs-imperial/occam-dom"
  },
  "dependencies": {
    "necessary": "^4.1.10"
  },
  "devDependencies": {
    "@babel/core": "^7.8.6",
    "@babel/plugin-proposal-class-properties": "^7.8.3",
    "@babel/plugin-transform-react-jsx": "^7.8.3",
    "@babel/preset-env": "^7.11.0",
    "browserify": "^16.5.1",
    "concurrently": "^5.2.0",
    "easy": "^11.1.7",
    "easy-layout": "^5.0.15",
    "easy-with-style": "^3.0.18",
    "express": "^4.17.1",
    "juxtapose": "^3.0.2",
    "lively-cli": "^1.1.17",
    "occam-grammars": "^1.0.22",
    "watchful-cli": "^1.4.6"
  },
  "scripts": {
    "start": "node ./bin/main.js",
    "clean": "rm -rf ./lib",
    "lively": "lively --port=3333 --watch-pattern=./example.js --allowed-origin=http://localhost:8888",
    "watchful": "watchful --source-directory=./es6 --temp-directory=./lib --entry-file=example.js --bundle-file=./example.js --pause=100",
    "batch": "npm run watchful batch --",
    "batch-debug": "npm run watchful batch -- --debug",
    "incremental": "npm run watchful incremental --",
    "incremental-debug": "npm run watchful incremental -- --debug",
    "build": "npm run clean && npm run batch",
    "build-debug": "npm run clean && npm run batch-debug",
    "watch": "npm run clean && npm run batch && concurrently -k 'npm run incremental' 'npm run lively'",
    "watch-debug": "npm run clean && npm run batch-debug && concurrently -k 'npm run incremental-debug' 'npm run lively'"
  },
  "main": "./lib/index.js"
}
